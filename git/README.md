# git

Git configuration.

| File | Description |
|------|-------------|
| `.gitconfig` | Global git config — user, delta pager, rebase pull, merge conflict style |
| `.gitignore_global` | Global gitignore |

Uses [delta](https://github.com/dandavison/delta) as the diff pager with side-by-side view.
